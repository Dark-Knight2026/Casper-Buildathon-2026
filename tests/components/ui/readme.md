# UI Primitive Component Tests

Unit tests for shared shadcn-based UI primitives.

| File | Responsibility |
|------|----------------|
| badge.test.tsx | Tests Badge variants (default, secondary, destructive, outline, success, info) — background, foreground, border classes |
| button.test.tsx | Tests Button white variant classes and loading prop (Loader2 spinner, aria-disabled, native disabled) |
