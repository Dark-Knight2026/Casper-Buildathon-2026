#cloud-config

merge_how:
 - name: list
   settings: [append]
 - name: dict
   settings: [no_replace, recurse_list]

users:
  - name: root
    shell: /bin/bash
    sudo: ["ALL=(ALL) NOPASSWD: ALL"]
    lock_passwd: true
    ssh_authorized_keys:
      - ${ssh_public_key}

ssh_pwauth: false

chpasswd:
  expire: false

package_update: true
package_upgrade: true

packages:
  - htop
  - apt-transport-https
  - ca-certificates
  - curl
  - software-properties-common
  - gnupg
  - make

write_files:
  - path: /etc/ssh/sshd_config.d/99-no-password.conf
    content: |
      PasswordAuthentication no
      PermitRootLogin prohibit-password

  - path: /root/rclone.conf
    permissions: '0755'
    owner: root:root
    content: |
      [gcs]
      type = google cloud storage
      service_account_file = /creds/service-account.json
      bucket_policy_only = true

  - path: /root/service_account.json.b64
    permissions: '0600'
    owner: root:root
    content: |
      ${service_account_creds_b64}

  - path: /root/init.sh
    permissions: '0700'
    owner: root:root
    content: |
      #!/bin/bash
      set -euo pipefail

      # decode service account creds
      base64 -d /root/service_account.json.b64 > /root/service_account.json
      chmod 600 /root/service_account.json

      # ===========================================================================================
      ## Firewall (ufw) install
      apt install -y ufw
      ufw default deny incoming
      ufw default allow outgoing
      ufw allow 22/tcp   # SSH
      ufw allow 80/tcp   # HTTP (certbot + redirect)
      ufw allow 443/tcp  # HTTPS
      ufw --force enable

      # ===========================================================================================
      ## Fail2ban install
      apt install -y fail2ban
      cat > /etc/fail2ban/jail.local <<'JAIL'
      [sshd]
      enabled  = true
      port     = ssh
      filter   = sshd
      logpath  = /var/log/auth.log
      maxretry = 5
      bantime  = 3600
      findtime = 600
      JAIL
      systemctl enable fail2ban
      systemctl restart fail2ban

      # ===========================================================================================
      ## Docker install
      echo "[INFO] Installing Docker..."
      mkdir -p /etc/apt/keyrings
      curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
      chmod a+r /etc/apt/keyrings/docker.gpg
      echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" > /etc/apt/sources.list.d/docker.list
      apt update
      apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
      systemctl enable docker
      systemctl start docker

      # ===========================================================================================
      ## Docker login
      echo "[INFO] Docker login..."
      cat /root/service_account.json | docker login -u _json_key --password-stdin https://${google_application_region}-docker.pkg.dev

      echo "Instance init completed."


runcmd:
  - mkdir -p /opt/itinerary_service/logs
  - /root/init.sh > /opt/itinerary_service/logs/01_deploy-init.log 2>&1
  - systemctl restart ssh
